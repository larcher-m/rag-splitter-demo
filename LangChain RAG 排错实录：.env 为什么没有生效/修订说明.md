# 修订说明

## 生成文件

- `LangChain-RAG-dotenv-为什么没有生效.md`：可发布的掘金 Markdown 正文。
- `LangChain-RAG-dotenv-cover.png`：1280 x 720 文章封面。
- `LangChain-RAG-dotenv-flow.png`：1280 x 720 配置优先级与请求链路图。

## 主要处理

- 以项目中 Cheerio 抓取、递归切分、向量化和内存检索代码为技术背景，整理成完整 RAG 调试案例。
- 将模糊的依赖内部 `TypeError` 追溯到原始接口响应，区分网关格式问题与真实的 `401 invalid_api_key`。
- 补充 dotenv 默认不覆盖已有 `process.env` 的关键机制，以及当前终端、用户环境变量和 `.env` 的关系。
- 添加可直接运行的脱敏诊断片段和 PowerShell 处理命令。
- 未收录 API Key、真实网关域名或其他敏感配置。
